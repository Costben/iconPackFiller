# Build artifacts
build/
.gradle/
local.properties
*.iml
.idea/
.kotlin/
captures/
.externalNativeBuild/
.cxx/
release.jks

# Test outputs / spike scratch
spike/
out/
*.apk.unaligned

# Working scratch, unrelated to build & tests (kept on disk, not in git)
# - design/: launcher-icon drafts (gen.py/finalize.py + PNG previews);
#   finished assets already live in app/src/main/res/mipmap-*
# - fixtures/: byte-identical duplicate of app/src/test/resources/test-iconpack.apk;
#   unit tests load the latter via classloader
design/
fixtures/